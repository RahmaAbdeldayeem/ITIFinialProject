@model IEnumerable<CenterApp.Core.Models.Level>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Level_Name)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Level_Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Level_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Level_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Level_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
